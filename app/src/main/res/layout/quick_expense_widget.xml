<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="4dp">

    <!-- The actual Icon Container (60dp x 60dp) -->
    <FrameLayout
        android:id="@+id/widget_container"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center_horizontal"
        android:background="@drawable/widget_background">

        <!-- Wallet Icon in the center -->
        <ImageView
            android:id="@+id/widget_icon"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_widget_wallet"
            android:contentDescription="Wallet" />

        <!-- Plus badge in the bottom-right corner -->
        <TextView
            android:id="@+id/widget_badge"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="2dp"
            android:background="@drawable/widget_plus_badge_background"
            android:text="+"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:textStyle="bold"
            android:gravity="center" />

    </FrameLayout>

    <!-- App Icon Label below -->
    <TextView
        android:id="@+id/widget_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="4dp"
        android:text="Wallet"
        android:textColor="#FFFFFF"
        android:textSize="12sp"
        android:singleLine="true"
        android:ellipsize="end"
        android:shadowColor="#80000000"
        android:shadowDx="1.5"
        android:shadowDy="1.5"
        android:shadowRadius="2" />

</LinearLayout>
